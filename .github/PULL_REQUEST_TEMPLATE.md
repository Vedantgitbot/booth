<!--
Thanks for working on BOOTH. A quick heads-up before you open this:
BOOTH is deliberately kept small (see the Philosophy section in README.md).
PRs that add new public functions/parameters/statuses without a prior
discussion in an issue are unlikely to be merged, please open an issue
first for anything beyond a bug fix, so we don't waste your effort on a PR
that doesn't land. Bug fixes and edge-case fixes with a reproduction are
always welcome directly.
-->

## What does this change?

<!-- One or two sentences. If this fixes an issue, link it: Fixes #123 -->

## Why?

<!-- What was broken, or what edge case does this handle? If you didn't
     open an issue first, briefly explain how you reproduced this. -->

## Checklist

- [ ] I ran the full test suite locally (`pytest -v`) and it passes.
- [ ] I added a new test that fails without this change and passes with it.
- [ ] This does not modify any existing test's behavior/assertions, unless that's the explicit point of this PR (explain above if so).
- [ ] If this changes public behavior, I added a `CHANGELOG.md` entry following the existing format.
- [ ] If this is a bug fix, I can point to how I reproduced the original bug (a script, an issue, a failing test).

## Anything else worth knowing?

<!-- Trade-offs you considered, alternatives you rejected, open questions. -->